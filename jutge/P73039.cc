#include <iostream>
#include <vector>
using namespace std;
using ms = vector<string>;


int main() {
	int n;
	cin >> n;
	ms M(10);
	for (int i = 0; i < n; ++i) {
		for (int j = 0; j < 10; ++j) {
			cin >> M[j];
		}
		if (M[k].last() != M[k+1].first)
	}
}
